command:
  - "streamlit"
  - "run"
  - "app.py"

env:
  # Flip to "false" once the app is deployed and you want it to write secrets
  # and emit real workflows. "true" keeps the app inert (safe for run-local).
  - name: USE_MOCK_BACKEND
    value: "true"
